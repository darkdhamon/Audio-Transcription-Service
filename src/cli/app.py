# this will be the CLI version of the app

# Does appsettings.json exist? if not create it
# Is default Recording directory defined in appsettings? if not ask user to choose one and define it.

# List recording sessions in recording directory. allow user to select session to transcribe. 
# each session is a subdirectory in the default recording folder.